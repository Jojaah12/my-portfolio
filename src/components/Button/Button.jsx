import "./Button.css";

import React from "react";

const Button = () => {
  return (
    <div class="stage">
      <button class="btn">View My Work</button>
    </div>
  );
};

export default Button;
